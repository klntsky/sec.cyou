<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SEC, SEE YOU</title>
    <link rel="stylesheet" href="./style.css"></link>
  </head>
  <body>
    <div class="outer">
    <div id="description">
      This is a catalogue of <a target="_blank" href="https://en.wikipedia.org/wiki/Decentralized_finance">DeFi</a> platforms that facilitate permissionless and <a target="_blank" href="https://en.wikipedia.org/wiki/Know_your_customer">KYC-free</a> trading.
    </div>

    <div id="container">
    <% for (let e of list) { %>
      <div class="item">
        <a href="<%= e.website %>" target="_blank"><span class="item-name"><%= e.name %></span></a>
        <div class="item-description"><%= e.description %></div>
        <div class="item-tags">
        <% for (let tag of e.tags) { %>
          <span class="item-tag" style="background-color: <%= colorHash(tag) %>;"><%= tag _%> </span>
        <% } %>
        </div>
        <% if (e.opensource) { %>
          <span class="opensource">open source</span>
        <% } else { %>
          <span class="closedsource">closed source</span>
        <% } %>
      </div>
    <% } %>
    </div>
    </div>
  </body>
</html>
